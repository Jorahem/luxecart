<main class="px-4 py-8">
  <div class="max-w-6xl mx-auto">
    <!-- Header -->
    <div class="flex flex-col lg:flex-row lg:items-end lg:justify-between gap-4 mb-6">
      <div>
        <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full border border-white/10 bg-white/5 text-xs font-semibold text-slate-200 mb-3">
          <span class="h-2 w-2 rounded-full" style="background: rgba(99,102,241,.95)"></span>
          Premium Catalog
        </div>
        <h1 class="text-3xl md:text-4xl font-extrabold text-white tracking-tight">Products</h1>
        <p class="text-sm text-slate-300 mt-2">
          Manage products, featured items, pricing, and inventory.
        </p>
      </div>

      <div class="flex items-center gap-2">
        <%= link_to "New Product",
              new_admin_product_path,
              class: "px-4 py-2 rounded-xl text-white text-sm font-semibold shadow",
              style: "background: linear-gradient(135deg, rgba(99,102,241,.92), rgba(217,70,239,.92));" %>

        <%= link_to admin_dashboard_path,
              class: "px-4 py-2 rounded-xl border border-white/10 bg-white/5 hover:bg-white/10 text-sm font-semibold text-white" do %>
          <i class="fas fa-arrow-left mr-2"></i> Back
        <% end %>
      </div>
    </div>

    <!-- Search / Filters -->
    <div class="rounded-2xl border border-white/10 p-4 mb-6" style="background: rgba(255,255,255,.04);">
      <div class="flex flex-col md:flex-row md:items-center gap-3">
        <div class="flex-1">
          <label class="block text-xs font-semibold text-slate-300 mb-2">Search</label>
          <input type="text"
                 placeholder="Search by name, SKU, brand..."
                 class="w-full px-4 py-3 rounded-xl bg-white text-black border border-white/10 focus:outline-none focus:ring-2 focus:ring-indigo-500" />
        </div>

        <div class="flex items-end gap-2">
          <div>
            <label class="block text-xs font-semibold text-slate-300 mb-2">Quick</label>
            <div class="flex items-center gap-2">
              <span class="px-3 py-2 rounded-xl text-xs font-semibold border border-white/10 bg-white/5 text-white">
                Total: <%= @products.respond_to?(:count) ? @products.count : 0 %>
              </span>
              <% if defined?(Product) && Product.column_names.include?("featured") %>
                <span class="px-3 py-2 rounded-xl text-xs font-semibold border border-white/10 bg-white/5 text-white">
                  Featured: <%= Product.where(featured: true).count %>
                </span>
              <% end %>
            </div>
          </div>
        </div>
      </div>

      <p class="text-xs text-slate-400 mt-3">
        Tip: I can connect this search to the controller using params for real filtering.
      </p>
    </div>

    <!-- Table -->
    <div class="rounded-2xl border border-white/10 bg-white/5 overflow-hidden">
      <div class="px-5 py-4 border-b border-white/10 flex items-center justify-between">
        <div>
          <h2 class="text-white font-bold text-lg">All Products</h2>
          <p class="text-xs text-slate-400 mt-1">Fast actions + clean overview</p>
        </div>
      </div>

      <div class="overflow-x-auto">
        <table class="min-w-full text-sm">
          <thead style="background: rgba(255,255,255,.04);">
            <tr class="text-left text-xs uppercase tracking-wider text-slate-300">
              <th class="px-4 py-3">Product</th>
              <th class="px-4 py-3">Category</th>
              <th class="px-4 py-3">Brand</th>
              <th class="px-4 py-3">Price</th>
              <th class="px-4 py-3">Featured</th>
              <th class="px-4 py-3 text-right">Actions</th>
            </tr>
          </thead>

          <tbody class="divide-y divide-white/5">
            <% if @products.blank? %>
              <tr>
                <td colspan="6" class="px-4 py-10 text-center text-slate-300">
                  No products found.
                </td>
              </tr>
            <% else %>
              <% @products.each do |p| %>
                <% featured = p.respond_to?(:featured) ? !!p.featured : false %>

                <tr class="hover:bg-white/5">
                  <td class="px-4 py-4">
                    <div class="flex items-center gap-3">
                      <!-- Image -->
                      <div class="h-10 w-10 rounded-xl border border-white/10 overflow-hidden flex items-center justify-center"
                           style="background: rgba(255,255,255,.04);">
                        <% if p.respond_to?(:image) && p.image.present? %>
                          <%= image_tag p.image, class: "h-full w-full object-cover" %>
                        <% elsif p.respond_to?(:images) && p.images.respond_to?(:attached?) && p.images.attached? %>
                          <%= image_tag url_for(p.images.first), class: "h-full w-full object-cover" %>
                        <% else %>
                          <span class="text-slate-400 text-xs">IMG</span>
                        <% end %>
                      </div>

                      <div class="min-w-0">
                        <div class="text-white font-semibold truncate"><%= p.try(:name) || "Product ##{p.id}" %></div>
                        <div class="text-xs text-slate-400 truncate">
                          ID: <%= p.id %>
                          <% if p.respond_to?(:sku) && p.sku.present? %> • SKU: <%= p.sku %><% end %>
                        </div>
                      </div>
                    </div>
                  </td>

                  <td class="px-4 py-4 text-slate-200">
                    <%= p.try(:category).try(:name) || (p.respond_to?(:category_name) ? p.category_name : "—") %>
                  </td>

                  <td class="px-4 py-4 text-slate-200">
                    <%= p.try(:brand).try(:name) || (p.respond_to?(:brand_name) ? p.brand_name : "—") %>
                  </td>

                  <td class="px-4 py-4 text-slate-200">
                    <% if p.respond_to?(:price) && p.price.present? %>
                      <%= number_to_currency(p.price) rescue p.price %>
                    <% elsif p.respond_to?(:price_cents) && p.price_cents.present? %>
                      <%= number_to_currency(p.price_cents.to_i / 100.0) %>
                    <% else %>
                      —
                    <% end %>
                  </td>

                  <td class="px-4 py-4">
                    <% if featured %>
                      <span class="inline-flex items-center px-2.5 py-1 rounded-full text-xs font-semibold"
                            style="background: rgba(16,185,129,.18); color: rgba(167,243,208,.95);">
                        Featured
                      </span>
                    <% else %>
                      <span class="inline-flex items-center px-2.5 py-1 rounded-full text-xs font-semibold"
                            style="background: rgba(148,163,184,.18); color: rgba(226,232,240,.95);">
                        Normal
                      </span>
                    <% end %>
                  </td>

                  <td class="px-4 py-4">
                    <div class="flex items-center justify-end gap-2">
                      <%= link_to "View",
                            admin_product_path(p),
                            class: "px-3 py-2 rounded-xl border border-white/10 bg-white/5 hover:bg-white/10 text-xs font-semibold text-white" %>

                      <%= link_to "Edit",
                            edit_admin_product_path(p),
                            class: "px-3 py-2 rounded-xl border border-white/10 bg-white/5 hover:bg-white/10 text-xs font-semibold text-white" %>

                      <%# Toggle featured (your routes show toggle_featured_admin_product_path) %>
                      <%= link_to "Toggle",
                            toggle_featured_admin_product_path(p),
                            data: { turbo_method: :patch },
                            class: "px-3 py-2 rounded-xl text-xs font-semibold text-white",
                            style: "background: linear-gradient(135deg, rgba(56,189,248,.85), rgba(99,102,241,.85));" %>

                      <%= button_to "Delete",
                            admin_product_path(p),
                            method: :delete,
                            form: { data: { turbo_confirm: "Delete this product permanently?" } },
                            class: "px-3 py-2 rounded-xl text-xs font-semibold text-white",
                            style: "background: rgba(239,68,68,.85);" %>
                    </div>
                  </td>
                </tr>
              <% end %>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>

    <div class="mt-6 text-xs text-slate-400">
      Want premium filters (category dropdown, brand dropdown, featured toggle, price range) + real search? Tell me and I’ll wire it into the controller.
    </div>
  </div>
</main>