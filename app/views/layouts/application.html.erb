<!DOCTYPE html>
<html>
  <head>
    <title>LuxeCart - Premium E-commerce</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <!-- Navigation -->
    <nav class="bg-white shadow-lg">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <div class="flex-shrink-0 flex items-center">
              <%= link_to "LuxeCart", root_path, class: "text-2xl font-bold text-indigo-600" %>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
              <%= link_to "Products", products_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-900" %>
              <%= link_to "Categories", categories_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-500 hover:text-gray-900" %>
              <%= link_to "Brands", brands_path, class: "inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-500 hover:text-gray-900" %>
            </div>
          </div>
          <div class="flex items-center">
            <% if user_signed_in? %>
              <%= link_to "Cart (#{@cart.total_items})", cart_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2" %>
              <%= link_to "Wishlist", wishlist_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2" %>
              <%= link_to "Orders", orders_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2" %>
              <% if current_user.admin? %>
                <%= link_to "Admin", admin_root_path, class: "text-indigo-600 hover:text-indigo-900 px-3 py-2 font-semibold" %>
              <% end %>
              <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-gray-700 hover:text-gray-900 px-3 py-2" %>
            <% else %>
              <%= link_to "Sign In", new_user_session_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2" %>
              <%= link_to "Sign Up", new_user_registration_path, class: "bg-indigo-600 text-white px-4 py-2 rounded-md hover:bg-indigo-700" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% flash.each do |name, msg| %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="border-l-4 p-4 <%= flash_class(name) %>" role="alert">
          <p><%= msg %></p>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-white mt-12 border-t">
      <div class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
        <p class="text-center text-gray-500 text-sm">
          &copy; <%= Time.current.year %> LuxeCart. All rights reserved.
        </p>
      </div>
    </footer>
  </body>
</html>
