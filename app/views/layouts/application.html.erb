<!DOCTYPE html>
<html class="h-full bg-gray-50">
  <head>
    <title>LuxeCart | Premium E-Commerce</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>
  <body class="h-full flex flex-col">
    <nav class="bg-white shadow-sm border-b sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16 items-center">
          <div class="flex items-center gap-8">
            <%= link_to root_path, class: "text-2xl font-bold text-indigo-600 tracking-tight" do %>
              LUXE<span class="text-gray-900">CART</span>
            <% end %>
          </div>
          <div class="flex items-center space-x-4">
            <%= link_to "Cart", cart_path, class: "text-gray-600 hover:text-indigo-600 font-medium" %>
            <% if respond_to?(:user_signed_in?) && user_signed_in? %>
              <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "text-gray-600" %>
            <% else %>
              <%= link_to "Login", new_user_session_path, class: "text-gray-600" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>
    <main class="flex-grow">
      <%= yield %>
    </main>
    <footer class="bg-gray-900 text-white py-8 text-center">
      &copy; <%= Time.current.year %> LuxeCart
    </footer>
  </body>
</html>